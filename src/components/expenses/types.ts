export type ExpenseColumn = {
    id: string
    description: string
    amount: string
    category: string
    categoryId: string
    accountId: string
    accountName: string
    date: string
    rawDate: string
    createdAt: string
    imageUrl?: string | null
}
