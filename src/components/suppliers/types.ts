export type SupplierColumn = {
    id: string
    name: string
    contactPerson: string
    phone: string
    email: string
    address: string
    taxId: string
    balance?: number
    createdAt: string
}
